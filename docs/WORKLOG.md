# Work Log

이 문서는 프로젝트를 진행하면서 어떤 판단을 했고, 무엇을 만들었고, 다음에 무엇을 할지 기록하는 작업 로그입니다.

포트폴리오에서는 결과물만큼 과정이 중요하므로, 큰 작업이 끝날 때마다 이 문서를 업데이트합니다.

## Log Rule

각 기록은 아래 형식을 기준으로 남깁니다.

```md
## YYYY-MM-DD - 작업 제목

### 목표
- 이번 작업에서 해결하려는 문제

### 변경 사항
- 실제로 추가/수정한 내용

### 배운 점 / 이슈
- 시행착오, 판단 근거, 기술적 메모

### 다음 단계
- 이어서 할 작업
```

## 2026-03-13 - 저장소 초기 세팅

### 목표
- 프로젝트를 GitHub에 올리고 포트폴리오용 기본 문서 구조를 만든다.

### 변경 사항
- GitHub 공개 저장소 `hahark-ops/project-01-multimodal-document-ai-workbench` 생성
- 로컬 Git 저장소 초기화 및 첫 커밋 생성
- `.gitignore` 추가
- README를 포트폴리오형 구조로 재작성

### 배운 점 / 이슈
- 시작 단계에서는 구현보다 문제 정의와 기록 체계를 먼저 잡는 것이 중요하다.
- README에는 "이미 구현한 것"과 "앞으로 구현할 것"을 구분해서 적어야 신뢰도가 올라간다.

### 다음 단계
- 아키텍처 초안 작성
- 폴더 구조 설계
- 첫 구현 범위와 샘플 문서 유형 결정

## 2026-03-13 - 아키텍처 초안 및 구조 설계

### 목표
- 구현 전에 초기 기술 선택을 고정하고, 저장소 구조를 만들어 다음 작업을 바로 시작할 수 있게 한다.

### 변경 사항
- `Next.js + FastAPI + PyMuPDF + Tesseract + Qdrant` 조합으로 초기 스택 결정
- `docs/ARCHITECTURE.md` 추가
- `frontend`, `backend`, `infra`, `scripts`, `data` 폴더 구조 생성
- README에 초기 기술 선택과 저장소 구조 반영

### 배운 점 / 이슈
- 처음부터 모든 문서 유형을 다 처리하려고 하면 구현이 멈출 가능성이 크다.
- 그래서 1차 목표는 텍스트 중심 PDF와 근거 기반 응답 흐름을 먼저 완성하는 것으로 잡았다.
- AWS 지향 포트폴리오이더라도 로컬 개발 구조를 먼저 단순하게 만드는 편이 전체 완성 확률이 높다.

### 다음 단계
- 프론트엔드와 백엔드 기본 프로젝트 생성
- 업로드부터 파싱까지 이어지는 첫 기능 흐름 구현
- 샘플 문서와 평가 질문 초안 준비

## 2026-03-13 - 기본 프로젝트 뼈대 생성

### 목표
- 문서 설계만 남기지 않고, 다음 작업에서 바로 개발할 수 있는 프론트엔드/백엔드 시작점을 만든다.

### 변경 사항
- `frontend`에 Next.js 기반 초기 파일 추가
- `backend`에 FastAPI 기반 초기 파일 추가
- `infra/docker-compose.yml`에 Qdrant 로컬 실행 설정 추가
- 환경 변수 예시 파일 추가

### 배운 점 / 이슈
- 초기에 실행 뼈대를 만들어 두면 이후 작업이 "무엇부터 만들지"보다 "어느 파일에 구현할지"로 바뀌어 진행 속도가 빨라진다.
- RAG 프로젝트도 결국은 업로드, 파싱, 저장, 검색, 응답의 API 경계가 명확해야 구현이 단순해진다.

### 다음 단계
- PDF 업로드 API에 실제 저장 로직 추가
- PyMuPDF 기반 텍스트 추출 구현
- 프론트엔드 업로드 화면과 백엔드 API 연결

## 2026-03-13 - 첫 vertical slice 구현

### 목표
- 문서 업로드에서 파싱 결과 확인까지 이어지는 최소 동작 흐름을 실제로 연결한다.

### 변경 사항
- FastAPI에 PDF 업로드, 파일 저장, 문서 상세 조회 API 추가
- PyMuPDF 기반 페이지별 텍스트 추출 로직 추가
- 문서 메타데이터와 페이지 텍스트를 메모리 저장소에 보관하도록 구성
- Next.js 화면에 PDF 업로드 폼과 파싱 결과 표시 UI 추가
- 백엔드 pytest 3개 통과
- 프론트엔드 production build 통과

### 배운 점 / 이슈
- 초반 속도가 빠른 이유는 AI가 문서 정리와 뼈대 생성을 빠르게 도와주기 때문이다.
- 하지만 실제 구현 단계에서는 패키지 설정, 타입 오류, 빌드 도구 이슈처럼 사람이 확인해야 할 지점이 바로 생긴다.
- 이번에도 `uv` 패키징 설정과 Next.js Turbopack 이슈를 따로 정리해야 했고, 그래서 진짜 난도는 이제부터 올라간다.

### 다음 단계
- 파싱 결과를 chunk 데이터로 변환
- 벡터 저장소와 검색 연결
- 질문응답 API 설계 및 근거 응답 포맷 확정
